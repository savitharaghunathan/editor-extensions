import { extensionName } from '../utilities/utils';

export const solutionServerEnabled = `${extensionName}.solutionServer.enabled`;
export const solutionServerUrl = `${extensionName}.solutionServer.url`;
export const analyzerPath = `${extensionName}.analyzerPath`;
export const kaiCacheDir = `${extensionName}.genai.cacheDir`;
export const kaiDemoMode = `${extensionName}.genai.demoMode`;
